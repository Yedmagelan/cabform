@extends('layouts.admin') @section('title', 'Coupons') @section('breadcrumb')<li class="breadcrumb-item active">Coupons</li>@endsection
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h4 class="fw-700 mb-0"><i class="fas fa-tags text-cb-primary me-2"></i>Coupons de réduction</h4><a href="#" class="btn btn-cabform btn-cabform-primary btn-cabform-sm"><i class="fas fa-plus me-1"></i>Ajouter</a></div>
<div class="card-cabform"><div class="table-responsive"><table class="table table-cabform mb-0"><thead><tr><th>Code</th><th>Type</th><th>Valeur</th><th>Utilisations</th><th>Expiration</th><th>Statut</th></tr></thead><tbody>@forelse($coupons as $c)<tr><td class="fw-700 text-cb-primary">{{ $c->code }}</td><td>{{ ucfirst($c->type) }}</td><td>{{ $c->type === 'percentage' ? $c->value.'%' : number_format($c->value,0,',',' ').' FCFA' }}</td><td>{{ $c->used_count }}/{{ $c->max_uses ?? '∞' }}</td><td class="text-cb-muted" style="font-size:0.85rem;">{{ $c->expires_at?->format('d/m/Y') ?? 'Illimité' }}</td><td><span class="badge-cabform {{ $c->is_active ? 'badge-success' : 'badge-danger' }}">{{ $c->is_active ? 'Actif' : 'Inactif' }}</span></td></tr>@empty<tr><td colspan="6" class="text-center text-cb-muted py-4">Aucun coupon.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $coupons->links() }}</div></div>
@endsection
